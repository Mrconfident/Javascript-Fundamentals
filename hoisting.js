b()

console.log(a)

var a = 'Tarun'

function b () {
  console.log('Hii T')
}

// Hoisting means assigning value to Variables and Function in Execution Context(In Creation Phase)

// Now Function is totally copied and put as it is but the case is different for the variables
// (variables assigned a placeholder value in creation phase which is undefined)

// Next phase is execution phase where code is executed line by line(there values of variables get assigned)
